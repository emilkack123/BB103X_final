rule process_data:
    input:
        "resources/cleaned.fasta.txt"
    output:
        "results/cleaned.fasta.txt"
    shell:
        "cut -d' ' -f1,2 {input} > {output}"


include: "workflow/rules/ahmad.smk"
